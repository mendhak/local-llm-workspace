{
  "mcpServers": {
    "ddg-search": {
      "command": "/bin/sh",
      "args": [
        "-c",
        "if ! command -v uvx >/dev/null 2>&1; then curl -LsSf https://astral.sh/uv/install.sh | sh; fi && /root/.local/bin/uvx duckduckgo-mcp-server"
      ]
    },
    "fetch": {
      "command": "/bin/sh",
      "args": [
        "-c",
        "if ! command -v uvx >/dev/null 2>&1; then curl -LsSf https://astral.sh/uv/install.sh | sh; fi && /root/.local/bin/uvx mcp-server-fetch"
      ]
    }
  }
}